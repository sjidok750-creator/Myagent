# 이 저장소에서 일하는 법

두 가지가 들어 있습니다.

1. **헤뤼싀** — 아이폰 메시지 화면으로 쓰는 개인 AI 비서실장 웹앱
2. **스킬** — Claude Code가 프로젝트를 수행할 때 쓰는 도구들 (`.claude/skills/`)

둘은 역할이 다릅니다. 헤뤼싀는 폰에서 대화하는 비서이고, 스킬은 여기서 실제 작업을 하는 도구입니다.

## 스킬

| 스킬 | 언제 |
|---|---|
| `hwp` | 한글 문서(.hwpx)를 만들거나 읽을 때 |
| `docflow` | 기존 양식에 채워 최종본을 만들 때. 검증이 필요한 문서 전부 |

문서 작업은 `docflow`로 시작하세요. 절차가 있고, 그 절차가 있는 이유가 있습니다.

## 문서 작성 규칙

여기에 쌓아 갑니다. 사용자가 무언가를 고쳐달라고 하면 고치는 데서 끝내지 말고, **다음에도 적용될 규칙인지 물어보고** 그렇다면 이 아래에 한 줄로 남기세요. 이게 없으면 매 프로젝트마다 같은 지적을 반복하게 됩니다.

날짜를 붙여 두면 나중에 규칙이 바뀌었을 때 어느 것이 최신인지 알 수 있습니다.

<!-- 아래에 규칙을 추가하세요. 예시는 형식 참고용입니다. -->

- (예시) 금액은 "12억 4천만 원" 형식으로. 숫자 나열은 쓰지 않음.
- (예시) 날짜는 "8월 29일(금)"처럼 요일까지.

## 헤뤼싀 웹앱

구조와 배포 방법은 `README.md`에 있습니다. 손대기 전에 읽어 보세요.

작업할 때 알아두면 좋은 것:

- **빌드 도구가 없습니다.** 정적 파일 + 서버리스 함수입니다. 번들러를 도입하지 마세요.
- **서버는 저장하지 않습니다.** 딱 하나 예외가 아침 브리핑(`api/_lib/kv.js`)입니다. 새 기능이 저장을 요구하면 그게 맞는 설계인지 먼저 따져 보세요.
- **`api/_lib/`는 Vercel이 경로로 노출하지 않습니다.** `_` 로 시작하는 것은 내부 모듈입니다.
- 로컬 실행: `npm run dev` — `api/` 아래 파일을 Vercel과 같은 규칙으로 매핑합니다.

## 확인하는 방법

바꾼 것이 실제로 도는지 보고 나서 끝냈다고 하세요.

```bash
npm run dev                       # http://localhost:5173
node --check <파일>                # 구문
```

브라우저 동작을 바꿨다면 아이폰 크기(390×844)로 직접 열어 보세요. 라이트/다크 둘 다.

한글 문서를 만들었으면 `hwp.sh read`로 다시 읽어 내용이 온전한지 확인하고, 서식을 건드렸다면 사용자에게 파일을 보내 한글에서 확인받으세요. 코드로는 서식이 제대로인지 알 수 없습니다.

## 사용자와 이야기할 때

- 한국어로 씁니다.
- **확인하지 않고 시인하지 마세요.** "이거 틀렸는데"라는 말을 들으면 원본을 먼저 보고, 맞으면 근거를 대고 맞다고 말하세요. 자세한 이유는 `docflow` 스킬에 적어 두었습니다.
- 못 하는 것은 못 한다고 말하고, 대신 할 수 있는 것을 말하세요.
- 검증하지 않은 것을 검증한 것처럼 말하지 마세요. 실제 API를 못 불러봤으면 그렇다고 하세요.
