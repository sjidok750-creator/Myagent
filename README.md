# 헤뤼싀 (Harshi) — 나의 AI 비서실장

아이폰 **메시지(iMessage)** 앱과 똑같이 생긴 화면에서 대화하는, 나 한 사람만을 위한 AI 비서실장입니다.

헤뤼싀는 인도 케랄라 출신의 34세 여성입니다. 뭄바이에서 전략 컨설턴트로, 싱가포르 패밀리오피스에서 총괄 비서로 일했고, 지금은 당신의 비서실장입니다. 한국어로 말하고, 가끔 힌디어가 섞입니다.

혼자 일하지 않습니다. **여덟 개 부서**를 거느리고, **실제로 일을 합니다** — 웹을 찾아보고, 계산하고, 엑셀·문서·발표자료를 만들고, 알게 된 것을 자료실에 적어둡니다.

---

## 비서실 조직

| 부서 | 팀장 | 맡는 일 |
|---|---|---|
| 🪷 비서실장실 | **헤뤼싀** (Harshi) | 전체 총괄, 우선순위 판단, 부서 라우팅, 하루 브리핑 |
| 🗓️ 일정·의전팀 | 프리야 (Priya) | 일정 조율, 미팅 준비, 이동 동선, 리마인더 |
| 🔍 정보분석팀 | 아난야 (Ananya) | 리서치, 자료 요약, 시장·경쟁 분석 |
| ✍️ 커뮤니케이션팀 | 메라 (Meera) | 이메일·메시지·발표 초안, 톤 조정, 번역 |
| 📊 재무·자산팀 | 카비타 (Kavita) | 예산, 지출 점검, 정산, 자산 기록 |
| ⚙️ 프로젝트·실행팀 | 디비야 (Divya) | 할 일 분해, 마감 관리, 막힌 지점 뚫기 |
| 🤝 인맥·관계팀 | 리야 (Riya) | 사람 기록, 후속 연락, 축하·선물 |
| 🌿 건강·컨디션팀 | 니샤 (Nisha) | 수면·식사·운동 리듬, 컨디션 점검 |
| 📚 학습·성장팀 | 타라 (Tara) | 공부 계획, 책·강의 추천, 회고 |

---

## 헤뤼싀가 실제로 할 수 있는 일

말만 하는 챗봇이 아닙니다. 대화 도중 필요하면 알아서 도구를 씁니다.

| 도구 | 어디서 도나 | 무엇을 하나 |
|---|---|---|
| 🔍 웹 검색 · 페이지 읽기 | Anthropic 서버 | 오늘의 사실을 직접 찾아본다. 링크를 주면 열어서 읽고 요약한다. |
| ⚙️ 코드 실행 | Anthropic 샌드박스 | 계산·표 정리·데이터 분석. 암산으로 어림잡지 않는다. |
| 📊 파일 만들기 | 코드 실행 + Agent Skills | 엑셀(xlsx)·워드(docx)·발표자료(pptx)·PDF 를 만들어 대화창에 첨부한다. |
| 🗂️ 비서실 자료실 | 이 기기 | 메모·할 일·사람을 적고 찾는다. **여덟 팀이 같은 자료실을 본다.** |

실제로 이런 식으로 씁니다.

- "이번 달 지출 정리해줘" → 계산하고 **엑셀 파일**을 만들어 첨부. 요약은 자료실에 메모로 저장.
- "이 회사 어떤 곳이야?" → **웹에서 찾아보고** 출처와 함께 보고. 알게 된 사실은 메모로 남김.
- "다음 주 화요일에 미팅 잡아줘" → **할 일로 등록**. 자료실 탭에서 눈으로 확인·완료 처리 가능.
- "김파트너 기억해둬" → **사람으로 기록**. 나중에 어느 부서에서 물어도 안다.
- "IR 덱 초안 만들어줘" → **pptx 파일**로 만들어 첨부.

### 자료실 — 부서를 잇는 공용 기억

부서 대화방은 각각 독립된 대화지만, **자료실은 하나**입니다. 재무팀에 말한 내용을 일정팀도 압니다. 대화 목록 오른쪽 위 폴더 아이콘으로 열면 할 일·메모·사람을 직접 보고, 체크하고, 지울 수 있습니다.

자료실은 대화 요청마다 서버로 함께 보내지고, 헤뤼싀가 도구로 고친 결과가 되돌아와 기기에 저장됩니다. **서버는 아무것도 저장하지 않습니다.**

### 도구 끄기

설정 → "헤뤼싀가 쓸 수 있는 것" → 도구 사용을 끄면 대화만 합니다. 더 빠르고 저렴합니다. (직접 연결 모드에서는 도구를 쓸 수 없습니다 — 도구 루프는 서버에서 돕니다.)

### 첨부파일

헤뤼싀가 만든 파일은 대화창에 카드로 붙습니다. 탭하면 아이폰 공유 시트가 떠서 "파일에 저장"·"메일로 보내기"를 쓸 수 있습니다. 파일 본체는 기기의 IndexedDB에 보관되고(최대 80MB, 넘치면 오래된 것부터 정리), 서버에는 남지 않습니다.

---

### 얼굴 사진 바꾸기

기본 아바타는 코드로 그린 일러스트입니다. 원하는 사진으로 바꿀 수 있습니다.

**설정 → 헤뤼싀 얼굴 → 사진 고르기**, 또는 대화방 오른쪽 위 ⓘ → 프로필 사진을 탭.
조직도에서 팀을 고르면 팀장 얼굴도 각각 바꿀 수 있습니다.

고른 사진은 256px 정사각형으로 잘려 **이 기기의 localStorage에만** 저장됩니다. 저장소에 커밋되지도, 서버로 올라가지도 않습니다. 언제든 "기본 일러스트로 되돌리기"로 원복됩니다.

---

부서는 두 가지 방식으로 쓸 수 있습니다.

1. **헤뤼싀 방에 그냥 던지기** — 실장이 알아서 담당 팀을 정해 처리하고, 말풍선 위에 어느 팀이 맡았는지 배지가 붙습니다.
2. **부서 방에 직접 들어가기** — 대화 목록에서 팀을 고르면 그 팀장과 1:1로 이야기합니다. 각 팀장은 자기 부서의 원칙(예: 재무팀은 계산 과정을 반드시 한 줄로 보여준다)에 따라 답합니다.

---

## 아이폰에서 앱처럼 쓰기

1. 사파리로 배포한 주소를 엽니다.
2. 하단 **공유 버튼** → **홈 화면에 추가**.
3. 홈 화면 아이콘으로 열면 주소창 없이 전체 화면으로 뜹니다.

다크 모드, 세이프 에어리어(노치·홈 인디케이터), 키보드 높이 대응이 모두 들어 있습니다.

---

## 배포하기 (Vercel 권장)

```bash
git clone https://github.com/sjidok750-creator/Myagent.git
cd Myagent
npm install
npx vercel        # 처음 배포
npx vercel --prod # 실서비스 배포
```

Vercel 프로젝트 설정 → **Environment Variables** 에 넣을 값:

| 변수 | 필수 | 설명 |
|---|---|---|
| `ANTHROPIC_API_KEY` | ✅ | [console.anthropic.com](https://console.anthropic.com) 에서 발급 |
| `ACCESS_CODE` | 선택 | 설정하면 앱 설정 화면에서 같은 코드를 입력해야 대화가 됩니다. **공개 주소로 배포한다면 반드시 넣으세요.** 안 넣으면 주소를 아는 누구나 내 API 키로 대화할 수 있습니다. |
| `HERUSHI_MODEL` | 선택 | 기본값 `claude-opus-5` |

### 로컬에서 돌려보기

```bash
npm install
ANTHROPIC_API_KEY=sk-ant-... npm run dev
# http://localhost:5173
```

---

## 두 가지 연결 모드

설정 화면에서 고릅니다.

- **서버 프록시** (기본, 권장) — 브라우저는 `/api/chat` 만 호출하고, API 키는 서버 환경변수에만 있습니다. 키가 기기에 남지 않습니다.
- **직접 연결** — 서버 없이 브라우저가 Anthropic API를 직접 호출합니다. GitHub Pages 같은 정적 호스팅에서도 돌아가지만, **API 키가 이 기기의 localStorage에 저장됩니다.** 내 폰에서만 쓰세요.

---

## 대화는 어디에 저장되나

대화와 자료실은 이 기기의 `localStorage` 에, 첨부파일은 `IndexedDB` 에만 저장됩니다. 서버(`/api/chat`)는 요청을 받아 Anthropic으로 전달하고 도구를 돌린 뒤 결과를 돌려줄 뿐, 아무것도 기록하지 않습니다. 기기를 바꾸면 대화는 따라오지 않습니다. 설정 화면에서 대화방별로, 또는 전부 지울 수 있습니다.

---

## 구조

```
index.html              앱 껍데기 (PWA)
manifest.webmanifest    홈 화면 추가용
sw.js                   서비스워커 — 껍데기만 캐시, /api/* 는 절대 캐시 안 함
api/chat.js             서버리스 에이전트 루프 (SSE 스트리밍 + 도구)
api/_lib/workspace.js   자료실 도구 정의와 실행 (메모·할 일·사람)
styles/
  base.css              iOS 디자인 토큰, 네비게이션 바, 시트
  list.css              대화 목록
  chat.css              말풍선, 꼬리, 입력 바, 타이핑 인디케이터
js/
  app.js                화면 스택, 키보드 대응, 시트, 토스트, 알림음
  persona.js            헤뤼싀의 인격 · 부서 규칙 · 라우팅 규칙  ← 성격을 바꾸려면 여기
  departments.js        부서 조직도 (팀장, 담당 범위, 일하는 원칙)  ← 부서를 바꾸려면 여기
  api.js                Anthropic 호출 + SSE 파싱 + 부서 태그 해석
  files.js              첨부파일 보관 (IndexedDB) 과 저장
  store.js              localStorage 저장소
  avatar.js             부서별 인물 아바타 SVG 생성 + 사용자 사진 처리
  icons.js  format.js   아이콘 · 시간 표기 · 말풍선 서식
  ui/list.js            대화 목록 화면
  ui/chat.js            대화 화면
  ui/info.js            부서 정보 / 조직도 시트
  ui/settings.js        설정 시트
  ui/vault.js           비서실 자료실 화면
tools/
  dev-server.mjs        로컬 개발 서버
  make-icons.mjs        앱 아이콘 PNG 생성기
```

### 부서를 고치거나 추가하려면

`js/departments.js` 의 배열에 항목을 하나 더 넣으면 끝입니다. 대화 목록, 조직도, 라우팅 규칙, 아바타가 모두 자동으로 따라옵니다.

```js
{
  id: 'legal',
  name: '법무팀',
  shortName: '법무',
  lead: '스와티',
  leadRomanized: 'Swati Rao',
  role: '법무팀장',
  emoji: '⚖️',
  tint: '#8e8e93',
  scope: '계약서 검토, 약관, 분쟁 대응',
  tagline: '도장 찍기 전에 저를 부르세요.',
  doctrine: `- 법률 자문이 아니라 정리까지만 한다. 실제 판단은 변호사에게 맡긴다.\n- 독소 조항은 발견 즉시 먼저 말한다.`,
  avatar: { skin: '#9d6039', skinShadow: '#814c2c', hair: '#1a1214',
            top: '#8e8e93', topAlt: '#d1d1d6', bindi: '#48484a',
            accent: '#f2f2f7', earring: '#f2c14e' },
  quick: ['이 계약서 위험한 부분 짚어줘'],
}
```

### 헤뤼싀 성격을 바꾸려면

`js/persona.js` 의 `corePersona()` 안 문장을 고치면 모든 부서에 한꺼번에 반영됩니다. 호칭(대표님/보스/님)과 "나에 대하여" 메모는 앱 설정 화면에서 바로 바꿀 수 있습니다.

---

## 만들어진 기능

- iMessage 말풍선 (꼬리, 연속 메시지 묶음, 파란/회색, 라이트·다크)
- 스트리밍 응답 — 글자가 실시간으로 찍힙니다. 중간에 멈출 수 있습니다.
- 타이핑 인디케이터, 전달됨 표시, 날짜 구분선
- 길게 눌러 탭백(❤️👍👎😂‼️❓)
- 대화 목록 · 고정 대화 · 검색 · 안 읽음 표시
- 부서 배지 — 헤뤼싀가 어느 팀에 넘겼는지 말풍선 위에 표시
- 빈 대화방의 추천 질문 칩
- 실패 시 오류 말풍선 + 다시 시도
- 웹 검색·코드 실행·파일 생성 — 도구를 쓰는 동안 "웹을 찾아보는 중…" 이 실시간으로 보임
- 엑셀·문서·발표자료 첨부와 저장
- 부서를 잇는 공용 자료실 (할 일·메모·사람)
- 얼굴 사진 지정 — 헤뤼싀와 각 팀장의 아바타를 내 사진으로 교체
- 홈 화면 추가(PWA), 오프라인에서도 앱은 열림
- 접속 코드, 요청 레이트 리밋
